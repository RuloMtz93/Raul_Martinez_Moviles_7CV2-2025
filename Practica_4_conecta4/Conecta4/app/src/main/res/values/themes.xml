<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Tema de la app heredando de Material3 DayNight sin ActionBar -->
    <style name="Theme.conecta4" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Opcional: colores de sistema transparentes -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
